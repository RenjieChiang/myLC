//
// Created by chiang on 2021/5/18.
//

#include "LCPMiddleArray2.h"
#include "algorithm"
#include "iostream"
int LCPMiddleArray2::threeSumClosest(std::vector<int> &nums, int target)
{
    std::sort(nums.begin(), nums.end());
    int closest = INT32_MAX;
    for (int i = 0; i < nums.size()-2; ++i)
    {

    }
    return 0;
}
